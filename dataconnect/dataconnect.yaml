specVersion: "v1"
serviceId: "pointage-app-firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pointage-app-firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
